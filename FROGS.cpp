#include <bits/stdc++.h>
using namespace std;

int main(void)
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);

    cin.ignore(numeric_limits<streamsize>::max(), '\n');
    return 0;
}